apiVersion: v1
kind: Service
metadata:
  name: spring-service
  labels:
    app: spring          
spec:
  type: LoadBalancer
  selector:
    app: spring          
  ports:
  - name: http           
    port: 80
    targetPort: 8080
